---
alwaysApply: true
---
# 人性化交互提示语

面向用户的文案（Toast / MessageBox / loading / 空态 / 表单校验 / API 业务异常文案等）必须口语、可行动，禁止技术语言。

## 适用范围

- 前端：`EleMessage` / `ElMessage` / `ElMessageBox`、loading、空状态、按钮确认文案、表单 `message`
- 后端：会返回给前端展示的 `BizException` 等业务异常文案
- 不要求改动：仅写日志、监控、内部调试信息

## 禁止出现

- 技术过程词：`请求中`、`接口失败`、`网络异常`（可改为「加载失败，请重试」等）
- 协议 / 状态码：`HTTP 500`、`404`、`timeout`、`ECONNREFUSED`
- 业务内部编码：`status=0`、错误码 `ERR_xxx`、字段名 `waybillNo`、SQL / 堆栈 / 异常类名
- 英文技术短语直接展示（除非品牌名、专有产品名）

## 推荐写法

| 场景 | ❌ 避免 | ✅ 推荐 |
|------|--------|--------|
| Loading | 请求中.. | 正在确认计划，请稍候… |
| 批量操作 | 处理中 | 正在批量确认，请稍候… |
| 成功 | OK / 操作成功 | 已成功确认 12 条计划 |
| 业务失败 | 状态不正确 | 仅「待调度」的计划可确认 |
| 系统失败 | Internal Server Error | 操作失败，请稍后重试；若持续出现请联系管理员 |
| 校验 | id 不能为空 | 请选择要确认的计划 |

原则：

1. **说清在做什么**：loading / 成功文案尽量带业务动作（确认、删除、导出…），少用空泛的「处理中」。
2. **说清下一步**：失败时给出可行动建议（重试、刷新、联系管理员）。
3. **用「请稍候」**：等待场景用「请稍候」，不用「请稍后」。
4. **后端异常即用户文案**：`BizException("…")` 的字符串会直接展示，按产品口吻写，不要塞编码或字段名。
5. **前端勿原样甩技术错误**：捕获后映射为友好文案；详细错误只打日志。

## 自检

写提示语时问：去掉上下文后，非研发同事能否立刻看懂、知道该做什么？若不能，重写。
