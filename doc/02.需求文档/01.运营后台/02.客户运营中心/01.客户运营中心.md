# 客户运营中心

## 一、业务背景

产品模式调整：移除免费版（basic），用户注册成功后自动开通**标准版（standard）**，给予 **15 天试用期**。试用期结束后版本失效，客户付费后继续使用。

基于此，客户运营中心由原来的 6 个子菜单（新注册客户、免费体验客户、跟进池、付费客户、流失客户、全量客户）精简为 **3 个**：

| 菜单 | 说明 |
|------|------|
| 试用期客户 | 注册后处于 15 天标准版试用期内的客户 |
| 付费客户 | 已付费开通版本的客户，支持按版本 Tab 切换查看 |
| 流失客户 | 试用到期未付费 或 付费到期未续费 的客户 |

## 二、客户生命周期

```
注册 ──自动开通标准版(15天试用)──> 试用期客户
                                    │
                          ┌─────────┴─────────┐
                          │试用期内付费         │试用期结束未付费
                          ▼                    ▼
                       付费客户 ────到期未续费──> 流失客户
                          ▲                    │
                          │                    │
                          └──── 重新付费 ───────┘
```

### 2.1 试用期客户

- **筛选条件**：`status=1` 且仅有 standard 版本的试用授权，且 `expire_time > NOW()`
- **说明**：注册时自动开通标准版，试用期 15 天，试用期内可升级为付费版本
- **运营用途**：引导客户在试用期内完成付费转化

### 2.2 付费客户

- **筛选条件**：`status=1` 且拥有已付费的有效授权
- **子分类（Tab 切换）**：
  - 全部：所有付费客户
  - 标准版：有效授权中包含 `version_code='standard'` 的已付费客户
  - 专业版：有效授权中包含 `version_code='pro'`
  - 旗舰版：有效授权中包含 `version_code='enterprise'`
  - 到期预警：`expire_time` 距今 <= 30 天
- **运营用途**：续费管理、版本升级、增值服务

### 2.3 流失客户

- **筛选条件**：`status IN (0, 3)`（停用或已过期）
- **流入方式**：
  - 试用期结束未付费（status=3）
  - 付费版本到期未续费（status=3）
  - 手动停用（status=0）
- **运营用途**：客户召回

## 三、页面功能设计

### 3.1 试用期客户

#### 搜索区域

| 字段 | 类型 | 说明 |
|------|------|------|
| 关键词 | 文本输入 | 企业名称/编码/联系人模糊搜索 |

#### 工具栏

- 注册企业：新增企业并自动开通标准版试用

#### 表格列

| 列名 | 字段 | 宽度 | 说明 |
|------|------|------|------|
| 企业编码 | tenantCode | 110 | |
| 企业名称 | tenantName | 160+ | |
| 联系人 | contactPerson | 110 | |
| 联系电话 | contactPhone | 140 | |
| 状态 | status | 100 | 正常/停用/已过期 |
| 来源渠道 | sourceChannel | 110 | 官网注册/后台录入/企业推荐 |
| 试用剩余 | - | 110 | 根据 expireTime 计算剩余天数 |
| 到期时间 | expireTime | 170 | |
| 创建时间 | createTime | 170 | |
| 操作 | - | 200 | 编辑、升级版本、停用 |

#### 操作按钮

- **编辑**：修改企业基本信息
- **升级版本**：为客户开通付费版本（standard/pro/enterprise）
- **停用**：手动停用客户（进入流失客户）

### 3.2 付费客户

#### 搜索区域

| 字段 | 类型 | 说明 |
|------|------|------|
| 关键词 | 文本输入 | 企业名称/编码/联系人模糊搜索 |

#### Tab 切换

顶部 Tab 栏切换不同版本视图，每个 Tab 显示对应数量角标：

- 全部 / 标准版 / 专业版 / 旗舰版 / 到期预警

#### 表格列

| 列名 | 字段 | 宽度 | 说明 |
|------|------|------|------|
| 企业编码 | tenantCode | 110 | |
| 企业名称 | tenantName | 160+ | |
| 联系人 | contactPerson | 110 | |
| 联系电话 | contactPhone | 140 | |
| 状态 | status | 100 | |
| 来源渠道 | sourceChannel | 110 | |
| 到期时间 | expireTime | 170 | 到期预警时标红 |
| 创建时间 | createTime | 170 | |
| 操作 | - | 200 | 编辑、授权管理、停用 |

#### 操作按钮

- **编辑**：修改企业基本信息
- **授权管理**：管理客户的产品版本授权（查看/新增/取消授权）
- **停用**：手动停用客户

### 3.3 流失客户

#### 搜索区域

| 字段 | 类型 | 说明 |
|------|------|------|
| 关键词 | 文本输入 | 企业名称/编码/联系人模糊搜索 |

#### 表格列

| 列名 | 字段 | 宽度 | 说明 |
|------|------|------|------|
| 企业编码 | tenantCode | 110 | |
| 企业名称 | tenantName | 160+ | |
| 联系人 | contactPerson | 110 | |
| 联系电话 | contactPhone | 140 | |
| 状态 | status | 100 | 停用/已过期 |
| 来源渠道 | sourceChannel | 110 | |
| 到期时间 | expireTime | 170 | |
| 创建时间 | createTime | 170 | |
| 操作 | - | 200 | 编辑、重新激活 |

#### 操作按钮

- **编辑**：修改企业基本信息
- **重新激活**：恢复客户状态，需为客户重新开通版本授权

## 四、数据流转规则

### 4.1 注册 → 试用期客户

- **触发**：调用 `POST /tenant` 创建客户
- **自动行为**：
  1. 创建 `sys_tenant` 记录，`status=1`
  2. 查询 `sys_product_version` 获取 standard 版本
  3. 自动创建 `sys_tenant_product` 记录（standard 版本，`end_time = 注册时间 + 15天`）
  4. 设置 `sys_tenant.expire_time = 注册时间 + 15天`
  5. 创建管理员账号和用户-企业关联

### 4.2 试用期客户 → 付费客户

- **触发**：运营后台为客户开通付费版本（`POST /tenant/{id}/products`）
- **自动行为**：
  1. 创建 `sys_tenant_product` 记录（付费版本，含到期时间）
  2. 更新 `sys_tenant.expire_time` 为所有授权中最晚的到期时间
  3. 标记为已付费

### 4.3 付费客户 → 续费

- **触发**：修改现有授权的到期时间，或重新开通同版本授权
- **自动行为**：更新 `sys_tenant.expire_time`

### 4.4 试用到期/付费到期 → 流失客户

- **触发条件**：试用期结束未付费 或 所有付费版本授权到期
- **检测方式**：
  1. 用户登录时检查
  2. `POST /tenant/check-expirations` 手动触发过期检查
  3. 定时任务（每日执行）
- **自动行为**：`sys_tenant.status` 设为 `3`（已过期）

### 4.5 流失客户 → 重新激活

- **触发**：运营后台点击"重新激活"或为客户开通新版本
- **自动行为**：
  - `status` 恢复为 `1`
  - 为客户开通版本授权

## 五、接口列表

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /tenant | 注册客户（自动开通标准版 15 天试用） |
| GET | /tenant/page | 分页查询（支持 lifecycle 筛选：trial/paid/churned） |
| GET | /tenant/{id} | 获取客户详情 |
| PUT | /tenant | 修改企业信息 |
| DELETE | /tenant/batch | 批量删除企业 |
| GET | /tenant/stats | 各阶段客户数量统计 |
| PUT | /tenant/status | 启用/停用（重新激活） |
| POST | /tenant/{id}/products | 开通产品版本（过期客户自动恢复） |
| DELETE | /tenant/{id}/products/{pid} | 取消授权（自动回退状态） |
| POST | /tenant/check-expirations | 手动触发过期检查 |
