# <模块名> 测试用例

> **对应需求**：`doc/02.需求文档/.../xxx.md`
> **对应后端**：`backend/app/modules/<端>/api/xxx.py`
> **对应前端**：`frontend/<端>/src/views/xxx`
> **测试脚本**：`backend/tests/<端>/test_xxx.py`

## 接口清单

| 方法 | 路径 | 说明 | 认证角色 |
|---|---|---|---|
| GET | `/api/.../xxx` | 列表 | ... |
| POST | `/api/.../xxx` | 新增 | ... |

## 测试用例

### TC-<端>-<模块>-001 <用例标题>

- **对应接口**：`POST /api/.../xxx`
- **优先级**：P0
- **用例类型**：正向
- **前置条件**：以 xxx 角色登录；已存在 xxx 数据
- **测试步骤**：
  1. ...
  2. ...
- **测试数据**：`{ ... }`
- **预期结果**：HTTP 200，`code=0`，返回体含 `id`；数据库新增一条记录
- **自动化状态**：已覆盖脚本
- **执行结果**：⬜ 未执行

---

### TC-<端>-<模块>-002 <反向：参数校验>

- **对应接口**：`POST /api/.../xxx`
- **优先级**：P1
- **用例类型**：反向（参数校验）
- **前置条件**：...
- **测试步骤**：提交缺失必填字段的请求
- **预期结果**：HTTP 422/400，返回明确错误信息
- **自动化状态**：待补
- **执行结果**：⬜ 未执行
