# AI 数字员工 测试用例

> **对应需求**：`项目文档/02.需求文档/01.运营后台/**`（AI 数字员工平台配置）
> **对应后端**：`backend/app/modules/ai/api/console/{employee,provider,prompt,tool,observe}.py`
> **对应前端**：`frontend/console/src/views`（AI 数字员工）
> **测试脚本**：`backend/tests/console/test_ai_console.py`

## 接口清单

| 方法 | 路径 | 说明 | 认证角色 |
|---|---|---|---|
| GET | `/api/console/ai/employee` | 数字员工分页 | 已登录 |
| GET | `/api/console/ai/employee/{id}` | 员工详情 | 已登录 |
| POST/PUT/DELETE | `/api/console/ai/employee*` | 员工 CRUD | 已登录 |
| GET/POST/PUT/DELETE | `/api/console/ai/provider*` | LLM Provider 管理 | 已登录 |
| GET/POST/PUT/DELETE | `/api/console/ai/prompt*` | 提示词模板 | 已登录 |
| GET | `/api/console/ai/tool` | 工具分页 | 已登录 |
| GET | `/api/console/ai/tool/categories` | 工具分类 | 已登录 |
| PUT/POST | `/api/console/ai/tool/{id}/status`、`/sync` | 启停/同步 | 已登录 |
| GET | `/api/console/ai/observe/*` | 调用观测（按租户库） | 已登录（待补脚本） |

## 数字员工

### TC-CON-AIEMP-001 数字员工分页
- **对应接口**：`GET /api/console/ai/employee` ｜P0 ｜正向
- **预期结果**：HTTP 200，`code=0`，`data.list` 存在
- **执行结果**：✅ 通过

### TC-CON-AIEMP-002 新增员工→详情→删除（回滚不落库）
- **对应接口**：`POST /api/console/ai/employee` + `GET .../{id}` + `DELETE .../{id}`
- **P0 ｜正向**：创建随机 code → 详情 `code` 一致 → 删除，各步 `code=0`
- **执行结果**：✅ 通过

### TC-CON-AIEMP-003 创建缺失必填 code
- **P1 ｜反向（参数校验）**：仅传 `name` → HTTP 422
- **执行结果**：✅ 通过

### TC-CON-AIEMP-004 未认证访问员工列表
- **P0 ｜反向（权限）**：无 token → HTTP 401
- **执行结果**：✅ 通过

### TC-CON-AIEMP-005 查询不存在员工
- **对应接口**：`GET /api/console/ai/employee/99999999` ｜P1 ｜反向
- **预期结果**：HTTP 200，`code!=0`
- **执行结果**：✅ 通过

## Provider / Prompt / Tool

### TC-CON-AIPROV-001 LLM Provider 分页
- **对应接口**：`GET /api/console/ai/provider` ｜P1 ｜正向 ｜预期 `code=0`
- **备注**：Provider 表名为 `ai_model_provider`（非 `ai_provider`），已确认存在并可查询
- **执行结果**：✅ 通过

### TC-CON-AIPROMPT-001 提示词模板分页
- **对应接口**：`GET /api/console/ai/prompt` ｜P1 ｜正向 ｜预期 `code=0`
- **执行结果**：✅ 通过

### TC-CON-AITOOL-001 AI 工具分页
- **对应接口**：`GET /api/console/ai/tool` ｜P1 ｜正向 ｜预期 `code=0`
- **执行结果**：✅ 通过

### TC-CON-AITOOL-002 工具分类列表
- **对应接口**：`GET /api/console/ai/tool/categories` ｜P2 ｜正向
- **预期结果**：`code=0`，`data.list` 存在
- **执行结果**：✅ 通过

## 调用观测（待补）

### TC-CON-AIOBS-001 观测接口（待补脚本）
- **对应接口**：`GET /api/console/ai/observe/{tenants,tool-logs,stats,sessions}`
- **说明**：观测查询按**租户库**执行（`tenantCode` 必填），且直接走 `db_manager` 租户 Session（不经 `get_platform_db`，无法用平台库回滚基座覆盖）。本轮以手工验证为主，脚本待补。
- **自动化状态**：待补 ｜**执行结果**：⬜ 未执行
