# 基础数据与地区 测试用例

> **对应需求**：`doc/02.需求文档/01.运营后台/**`（数据字典 / 地区数据 / 基础数据）
> **对应后端**：`backend/app/modules/console/api/dictionary/dict.py`、`region/region.py`、`basicdata/*`
> **对应前端**：`frontend/console/src/views`（数据字典 / 地区 / 品牌车系经销商）
> **测试脚本**：`backend/tests/console/test_system.py`

## 接口清单

| 方法 | 路径 | 说明 | 认证角色 |
|---|---|---|---|
| GET | `/api/console/system/dictionary/page` | 字典分页 | 已登录 |
| GET | `/api/console/system/dictionary` | 字典列表 | 已登录 |
| POST/PUT/DELETE | `/api/console/system/dictionary*` | 字典 CRUD | 已登录 |
| GET | `/api/console/basic-data/region/nav-tree` | 省市两级树 | 已登录 |
| GET | `/api/console/basic-data/region/tree` | 懒加载子级 | 已登录 |
| GET | `/api/console/basic-data/region/search` | 名称搜索 | 已登录 |
| GET/POST/PUT/DELETE | `/api/console/basic-data/region*` | 地区 CRUD | 已登录 |
| GET | `/api/console/basic-data/{vehicle-brand,vehicle-series,dealer}*` | 品牌/车系/经销商 | 已登录（待补脚本） |

## 数据字典

### TC-CON-DICT-001 分页查询字典
- **对应接口**：`GET /api/console/system/dictionary/page` ｜P1 ｜正向 ｜预期 `code=0`
- **执行结果**：✅ 通过

### TC-CON-DICT-002 新增并删除字典（回滚不落库）
- **对应接口**：`POST /api/console/system/dictionary` + `DELETE .../{id}`
- **P0 ｜正向**：创建随机 dictCode → 分页可查 → 删除，各步 `code=0`
- **执行结果**：✅ 通过

### TC-CON-DICT-003 字典编码重复
- **P1 ｜反向（唯一性）**：重复 dictCode → 第二次 `code!=0`
- **执行结果**：✅ 通过

### TC-CON-DICT-004 未认证访问字典分页
- **P0 ｜反向（权限）**：无 token → HTTP 401
- **执行结果**：✅ 通过

## 地区数据

### TC-CON-REGION-001 省+市两级导航树
- **对应接口**：`GET /api/console/basic-data/region/nav-tree` ｜P0 ｜正向 ｜预期 `code=0`
- **执行结果**：✅ 通过

### TC-CON-REGION-002 拉取省级根节点
- **对应接口**：`GET /api/console/basic-data/region/tree`（不传 pcode）｜P1 ｜正向
- **预期结果**：`code=0`，`data` 为数组
- **执行结果**：✅ 通过

### TC-CON-REGION-003 按名称搜索地区
- **对应接口**：`GET /api/console/basic-data/region/search?name=北京` ｜P1 ｜正向 ｜预期 `code=0`
- **执行结果**：✅ 通过

### TC-CON-REGION-004 未认证访问地区导航树
- **P0 ｜反向（权限）**：无 token → HTTP 401
- **执行结果**：✅ 通过

## 品牌 / 车系 / 经销商（待补）

### TC-CON-BASIC-001 品牌/车系/经销商分页（待补脚本）
- **对应接口**：`GET /api/console/basic-data/vehicle-brand`、`vehicle-series`、`dealer`
- **P2 ｜正向**：分页读接口鉴权与结构校验
- **自动化状态**：待补 ｜**执行结果**：⬜ 未执行（手工可验证）
