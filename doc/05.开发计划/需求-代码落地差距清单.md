# 需求-代码落地差距清单（开发计划）

> **创建日期**：2026-07-06
>
> **更新日期**：2026-08-14（财务结算模块**收官测试**：新增 `backend/scripts/verify/` 三个回归脚本，只读 54 项 + 客户侧写链路 39 项 + 应付与资金写链路 94 项全通过，`drift 0`、菜单快照与库一致、前端构建与 lint 通过。写链路测试修出 4 个此前未暴露的缺陷：销项票建单与打款批次建单因 `planned_amount` NOT NULL 未占位而必然 500、销项票状态机缺「申请中→已开票」导致开票登记永远失败、对账行只改调整额会回落到乘积吃掉分位残差（客户侧与承运商侧各一处）。报告见 [财务结算模块测试报告_2026-08-14](../07.上线交付/报告/财务结算模块测试报告_2026-08-14.md)；前端交互冒烟仍未做，见报告 §七）
>
> **历史更新**：2026-08-14（财务结算模块：**第 0~4 期全部完成**——继第 2 期客户侧应收后，第 3 期落地应付三线（承运商对账 23 + 承运商结算 20 + 司机工资 26 + 进项票 14 路由，10 张表），第 4 期落地票据与资金（销项票 18 + 银行账户 10 + 打款批次与出纳台 18 + 经营核算 6 路由，6 张表）。累计 `api/finance/` 194 个路由、24 张财务表、16 项二级菜单与 124 个权限点已 seed 入库、14 个前端页面上线，`npm run build:app` 通过。4 个旧占位菜单与页面已软删清理。剩余缺口只有第 5 期主体内部往来（定价口径未拍板，未立项）与幂等键。见 [07.财务结算/00.模块总览](../02.需求文档/02.企业端/07.财务结算模块/00.模块总览.md) §7.3~§7.5）
>
> **历史更新**：2026-08-14（财务结算模块第 2 期客户侧应收 A~F 六段落地）
>
> **历史更新**：2026-08-13（财务结算模块第 2 期 A/B/C 三段后端落地）
>
> **历史更新**：2026-07-07（计费引擎「承运商运费自动计算引擎」**前后端全链路落地**：后端模型/服务/API/Worker/派车触发/成本引擎边界切分/`billing_carrier_freight` 功能门控+菜单快照 + 前端「承运商合同」页（列表/详情/承运价编辑/版本历史/受影响重算/运费试算）均已就绪，见 §2.6）

> **历史更新**：2026-07-07（计费引擎新增「承运商运费自动计算引擎」设计：承运商合同 → 承运价规则 → 运费记录，与客户收入侧对称、复用 `freight_matcher` 算法层，派车给承运商自动算整单运费并回填 `task.carrier_cost_amount`，见 §2.6）

> **历史更新**：2026-07-07（AI 录单数字人能力增强：附件注入上下文 + 运单图片视觉识别（`image.extract_waybill`）+ 客户缺失主动追问澄清 + `map_columns` 常量回填，打通 Excel/图片双录单闭环）
>
> **历史更新**：2026-07-07（计费引擎「支出成本自动计算」支出侧全链路落地；经营主体（BusinessEntity）概念落地，并扩展客户/承运商/运单「收入归属」维度，明确跨主体不拦截策略）
>
> **历史更新**：2026-07-06（驾驶员资金账户「往来账」模块落地：账户本体 + 手工流水 + 司机 H5 只读 + 预付单支付/撤销自动流水联动）
>
> **文档定位**：盘点"**需求文档已有、但代码尚未开发或开发进行中**"的模块，作为后续排期与资源投入的依据。已完整落地的模块只在 §三 状态总表中列出，不展开。
>
> **维护约定**：每完成一个缺口项，更新其状态并从 §二 待办清单移除；新识别的缺口追加到 §二。
>
> **状态标记**：✅ 已落地 ｜ 🔧 部分落地（开发进行中）｜ 📐 仅模型/设计 ｜ 📋 未开发（仅需求文档）
>
> **判定口径**：以 `backend/app/modules/**`（models / services / api）与 `frontend/*`（views）实际代码为准，逐一比对需求文档。

---

## 一、结论速览

- 绝大多数需求文档对应的代码**已经落地**：账号体系、客户运营中心、AI 数字员工/数字人、资源管理（车辆/驾驶员/运力中心/社会运力池）、计费引擎**收入侧**（运价合同/运费计算/线路）、**计费引擎支出侧（任务支出成本自动计算）**、合作伙伴（客户/承运商/平台互联）、运营调度（任务单/状态机/调令/装卸/回单）、审批中心、系统设置（水印）、运力宝（证照监控/承运商建档）、**驾驶员资金账户「往来账」（含预付单支付/撤销自动流水联动）**、**经营主体（BusinessEntity）多主体归属维度**、驾驶员 H5 端等。
- **财务结算模块第 0~4 期已全部落地**（2026-08-14）：应收（客户对账 / 结算 / 收款核销 / 账龄信用）、应付（承运商对账 / 承运商结算 / 司机工资 / 进项票）、票据与资金（销项票 / 银行账户 / 打款批次 / 出纳台 / 资金流水）、经营核算，加三个岗位工作台，共 24 张财务表、194 个路由、14 个前端页面。模块内剩余两项：**第 5 期主体内部往来**（关联交易定价口径未拍板，未立项，先在核算页把跨主体金额单列为只读明细）与**写操作幂等键**（仍是 schema 预留）。
- **核心缺口现在只剩一块**：**移动端管理侧 / 移动端 BI 看板 / LITE 端承运商运力上报**——需求文档已有，前端工程未建。

---

## 二、待办清单（需求已有、代码未开发或进行中）

### 2.1 财务结算模块（第 0~4 期已收官）

> **2026-08-14 收官**：第 0 期（六篇设计文档）、第 1 期（公共底座）、第 2 期（客户侧应收）、第 3 期（应付三线）、第 4 期（票据与资金 + 经营核算）全部完成，分段进度见 [00.模块总览](../02.需求文档/02.企业端/07.财务结算模块/00.模块总览.md) §7.2~§7.5，交付口径见 §7.5。下表保留各项的落地明细供追溯；仍未闭合的只有 #14 第 5 期与 #15 幂等键两项。

| # | 需求文档 | 期次 | 状态 | 缺口明细 | 依赖 |
|---|---------|:---:|------|---------|------|
| 1 | 财务通用基座后续件（服务层） | 1 | ✅ 已落地 | `base/finance_doc_service.py` `FinanceDocService`（单号 / 状态机 / 事件 / 按钮位）、`linkage/waybill_to_finance.py`（客户侧候选池、签收台数口径、`assert_unbound`、置脏文案）、`linkage/task_to_finance.py`（应付侧候选池、扣减额口径、互斥、置脏文案）均已建。任务级费用单出于向后兼容仍内联，新单据一律继承模板 | 支撑全部后续项 |
| 2 | [09.运输单与财务单一致性核对](../02.需求文档/02.企业端/07.财务结算模块/09.运输单与财务单一致性核对.md) | 1 | ✅ 已落地 | `biz_recon_diff` 已建表并本地应用（迁移 `20260813_002`）；`recon/consistency_checker.py` 已落地置脏、重挂检测、差异去重留痕、确认拦截、强制确认、失效、**互斥校验统一收口**（`assert_task_settle_exclusive`）；`diff_constants.py` + `schemas/finance/recon_diff.py` 已建；事件类型 16~20 已补。第 2 期补客户侧 `line_detector`（台数/金额/状态回退比对）与漏挂检测、差异 API、对账工作台 UI；**第 3 期补应付侧 `ReconBinding` 与行级检测器**（承运商对账按任务台数/金额/扣减比对） | 通用基座（✅）、计费引擎（✅） |
| 3 | [02.客户侧-对账与结算与发票](../02.需求文档/02.企业端/07.财务结算模块/02.客户侧-对账与结算与发票.md)（除发票） | 2 | ✅ 已落地 | `biz_customer_recon(_waybill_link)` / `biz_customer_settlement(_recon_link)` 已建表并实际应用（迁移 `20260813_003/004`）；对账单 service+API（23 路由）含候选池、行增删改、大额调整审批、确认与回签、回灌重算；结算单 service+API（14 路由）含拆并关联、审批、直登收款、撤销收款；`waybill.is_locked` 收妥锁定与撤销解锁经 `LockOrchestrator` 联动。前端 `/finance/customer-recon`、`/finance/customer-settlement` 两个页面（列表 + 详情抽屉 + 建单/调整/回签/差异处置弹窗）已上线 | #1、#2、运单签收聚合（✅） |
| 4 | [10.资金收付与出纳台](../02.需求文档/02.企业端/07.财务结算模块/10.资金收付与出纳台.md)（收款核销部分） | 2 | ✅ 已落地 | `biz_receipt_voucher` + `biz_receipt_settle_link` 已建表；收款单 service+API（12 路由）含到账登记、候选推荐与一键填满、核销与撤销核销、满额驱动结算单收款并锁运单、出纳台 KPI；事件 22/23 已用。服务落在 `customer/receipt_voucher_service.py`（非原计划的 `cashier/`，原因见文档 10 §9.1）；第 4 期确认不搬家，并补上银行账面余额联动。UI 入口在结算单详情与出纳台「待认领到账」Tab | #3 |
| 5 | [12.应收账龄与信用管控](../02.需求文档/02.企业端/07.财务结算模块/12.应收账龄与信用管控.md) | 2 | ✅ 已落地 | `biz_customer` 三列 + 结算单 `due_date` 覆盖列已建；`aging_service`（实时聚合、到期日按结算方式推导、分桶阈值租户可配 `finance.aging_buckets`、基准日可回溯、xlsx 导出）+ `credit_alert_service`（四级预警文案、高危写事件 26，**全程不拦截业务**）+ 5 个只读接口已落地；客户档案三字段读写与校验已通（额度/账期可显式清空）。前端 `/finance/ar-aging`（KPI + 分桶柱图 + 客户明细下钻 + 导出）、客户档案「结算与信用」分组、录单页信用提示条已上线。**顺延第 3 期**：派车确认与对账确认两处提示点（多客户归并展示待定，见文档 12 §4.1） | #3、#4 |
| 6 | [03.承运商侧-对账与结算](../02.需求文档/02.企业端/07.财务结算模块/03.承运商侧-对账与结算.md) | 3 | ✅ 已落地 | `biz_carrier_recon(_task_link)` / `biz_carrier_settlement_doc(_recon_link)` 已建表并实际应用；对账单 service+API（23 路由）含任务候选池、预付/补款扣减、行调整与审批门槛、承运商回签、回灌重算；结算单 service+API（20 路由）含对账单拆并、审批、付款登记（强制选承运商结算账户）、撤销付款；`task.is_locked` / `is_recon_bound` 经 `LockOrchestrator` 批量联动，与任务级最终结算单互斥。前端 `/finance/carrier-recon`、`/finance/carrier-settlement` 两页已上线 | #1、#2、任务级结算单（✅） |
| 7 | [04.自有司机工资单](../02.需求文档/02.企业端/07.财务结算模块/04.自有司机工资单.md) | 3 | ✅ 已落地 | `biz_driver_payroll(_task_link/_item)` 已建表；service+API（26 路由）含按司机与周期聚合任务提成、三类工资项（应发 / 扣减 / 抵账）、油气款抵账、发放登记联动司机资金账户、`payroll_settled` 标记。前端 `/finance/driver-payroll` 含工资条视图 | #1、驾驶员资金账户（✅）、承包结算配置（✅） |
| 8 | [11.进项发票管理](../02.需求文档/02.企业端/07.财务结算模块/11.进项发票管理.md) | 3 | ✅ 已落地 | `biz_vendor_invoice(_item/_settle_link)` 已建表；service+API（14 路由）含收票登记（票号+票代唯一提示可跳转）、多税率行、与承运商结算单 N:N 可逆核销、票款不符只警示不阻断、抵扣台账汇总。抵扣台账未单建 service（见文档 11 §8.1）。前端 `/finance/vendor-invoice` 已上线 | #6 |
| 9 | [02.客户侧](../02.需求文档/02.企业端/07.财务结算模块/02.客户侧-对账与结算与发票.md) 发票章节 | 4 | ✅ 已落地 | `biz_customer_invoice(_item/_settle_link)` 已建表；service+API（18 路由）含开票候选与待开票池、拆票、登记开票、作废、红冲（另生成红字票并回指原票）、结算单 `is_locked` 联动。前端 `/finance/customer-invoice` 双 Tab（台账 + 待开票池）已上线 | #3 |
| 10 | [10.资金收付与出纳台](../02.需求文档/02.企业端/07.财务结算模块/10.资金收付与出纳台.md)（账户与批次） | 4 | ✅ 已落地 | `biz_bank_account` + `biz_payment_batch(_item)` 已建表；银行账户 API（10 路由）含 CRUD、启停、余额校准（事件 27）、引用保护；打款批次与出纳台 API（18 路由）含跨类型候选池、建批、审批、逐笔成败执行（失败笔留批并记事件 28）、出纳台概览 / 资金流水 / 付款日历三个读接口。前端 `/finance/cashier-workbench`（4 Tab）、`/finance/fund-flow`、`/finance/bank-account` 已上线 | #3、#6、#7 |
| 11 | [08.岗位视角与工作台设计](../02.需求文档/02.企业端/07.财务结算模块/08.岗位视角与工作台设计.md) | 2/4 | ✅ 已落地 | 三个工作台全部上线：对账工作台（`recon/workbench_service.py`，5 张 KPI 卡 + 4 Tab）、出纳工作台（待打款 / 打款批次 / 待认领到账 / 付款日历）、票据工作台（待开票 / 待收票 / 在途申请，纯前端聚合既有接口）。通用骨架只抽了 `finance-kpi-cards`（不做 `finance-workbench-layout`，原因见文档 08 §六） | 各自数据面 |
| 12 | [13.经营核算口径](../02.需求文档/02.企业端/07.财务结算模块/13.经营核算口径.md) | 4 | ✅ 已落地 | `/finance/profit` 占位页已重做：`accounting/` 下三个文件（口径常量 / 台数分摊纯函数 / 核算服务）+ 6 个路由，含财务确认口径的 KPI、六维汇总、单据下钻、跨主体往来明细、xlsx 底稿导出。**两项刻意未做**：不回改 `insight/profit_service.py` 复用 `cost_allocator`（避免驾驶舱数字偏移）、暂不加两页互链（待 §4.3 文案定稿），见文档 13 §8.2 | #3、#6、#7、#8 |
| 13 | 菜单与 feature 入库 | 1~4 | ✅ 已落地 | 16 项二级菜单（新增 941~950、955~957；复用 314 改名「经营核算」、401、403 改 `sort_order`）与 124 个权限点（占用 1000~1283）已写入 `client_menu.json` 并 seed 入库，前端页面校验 0 缺失；4 个旧占位菜单（197/198/199/313）已软删并删除对应页面目录；feature `finance_cashier` 新增并关联 `pro` 版本，四个 feature 的 `required_tables` 已补齐全部 24 张表；11 个新 SVG 图标 + 图标映射 + 总览页卡片已补。**实际 id 与总览 §9.2 预分配的偏差**：951~954 被平台侧占用，司机工资单 / 收付款流水 / 银行账户改用 955 / 956 / 957 | 各期功能 |
| 14 | 主体内部往来（关联交易）结算 | 5 | ⏸ 未立项 | 定价口径（成本加成 / 内部协议价 / 收入分成）未拍板，先建表必返工。第 4 期已在经营核算页提供「跨主体往来明细」只读视图，让金额先可见 | #12 |
| 15 | 写操作幂等键 | — | 📋 未开发 | `idempotency_key` 请求头四期均只在 schema 预留，未实现校验。上线前建议至少覆盖付款登记、发薪、批次执行三类不可逆动作 | 全模块 |

> 参考：[01.财务单据体系与业务单据联动设计](../02.需求文档/02.企业端/07.财务结算模块/01.财务单据体系与业务单据联动设计.md) §9 已标注各文件的 ✅/🕒 落地状态；新增能力的表结构、口径与测试要点见 08~13 六篇文档。

### 2.2 经营主体（BusinessEntity）多主体归属

> 经营主体（法人/独立核算单元）已落地：`biz_business_entity` 表 + `BusinessEntityService`（默认主体维护/CRUD/删除引用保护）+ API + 前端「系统管理/经营主体」。资源侧（司机/车辆/运力/任务/费用单）与资金账户（`owner_type + owner_id + enterprise_id`）已按主体归属；本次进一步补齐**收入侧归属维度**（客户/承运商/运单默认主体带出）。以下为剩余缺口。

| # | 需求文档 | 状态 | 缺口明细 | 依赖 |
|---|---------|------|---------|------|
| 5 | [10.系统管理/02.经营主体管理](../02.需求文档/02.企业端/10.系统管理模块/02.经营主体管理.md) | 🔧 部分 | **已落地**：`biz_customer` / `biz_carrier` / `biz_waybill` 新增 `enterprise_id`（迁移 `20260706_005`）；运单创建从客户默认主体带出收入归属、承运商任务成本归属继承承运商档案主体；明确「跨主体不拦截」（收入主体≠成本主体时不阻断，如实两侧记账）。**待建**：前端客户/承运商/运单表单的经营主体选择器与跨主体软提示；应收单据按主体分账（并入 §2.1）。**内部往来（关联交易）结算引擎已明确暂不立项**——缺关联交易定价口径（成本加成 / 内部协议价 / 收入分成三选一未拍板），先建表必返工；第 4 期已在经营核算页把跨主体金额单列为只读的「跨主体往来明细」，先满足看数需求，见 [13.经营核算口径](../02.需求文档/02.企业端/07.财务结算模块/13.经营核算口径.md) §六 | 经营主体基座（✅）、双维归属（收入=运单 / 成本=任务，✅） |

### 2.3 移动端 / LITE 端

| # | 需求文档 | 状态 | 缺口明细 |
|---|---------|------|---------|
| 6 | [03.移动端/BI数据看板](../02.需求文档/03.移动端/BI数据看板.md) | 📋 未开发 | `frontend/driver-h5` 无 BI/看板页面；移动端数据看板未建 |
| 7 | [03.移动端/业务模块](../02.需求文档/03.移动端/业务模块.md) | 🔧 部分 | 驾驶员 H5 端已落地（登录/多企业切换/任务流转/财务收入/个人中心）；**管理人员移动端**（调度/审批移动侧）尚未建工程 |
| 8 | [03.LITE端/承运商运力上报](../02.需求文档/03.LITE端/承运商运力上报.md) | 📋 未开发 | 无 `frontend/lite` 工程；承运商轻量上报入口未建（后端 `carrier_capacity` / `carrier_invitation` 已具备承接能力） |

### 2.4 运力宝商业化

| # | 需求文档 | 状态 | 缺口明细 |
|---|---------|------|---------|
| 9 | [06.运力宝/04.商业化基建-功能守卫与配额](../02.需求文档/06.运力宝/04.商业化基建-功能守卫与配额.md) | 🔧 部分 | `core/permissions.py` 的 `require_feature` 功能门控 + `quota_service` 版本配额校验已落地；**按运力宝业务定义的配额项、超限提示、商业化埋点/守卫覆盖面**待补全并逐接口接入 |
| 10 | [06.运力宝/01.产品方案与商业化设计](../02.需求文档/06.运力宝/01.产品方案与商业化设计.md) | 🔧 部分 | 证照监控（✅ `compliance`）、承运商运力建档（✅ `carrier_capacity`）已落地；**商业化转化闭环（试用/购买/额度售卖）**待确认落地范围 |

> 运力宝证照监控引擎（02）、承运商运力建档（03）已落地；[05.部署与运维手册](../02.需求文档/06.运力宝/05.部署与运维手册.md) 属运维文档，非代码开发项。

### 2.5 AI 数字人 - 录单员能力增强

> 2026-07-07 已落地「附件注入 + 运单图片视觉识别 + 追问澄清」核心闭环（详见 [06.AI 数字人模块/00.模块总览](../02.需求文档/02.企业端/06.AI%20数字人模块/00.模块总览.md) §十）。以下为剩余后续件。

| # | 需求文档 | 状态 | 缺口明细 | 依赖 |
|---|---------|------|---------|------|
| 11 | [06.AI 数字人模块/00.模块总览](../02.需求文档/02.企业端/06.AI%20数字人模块/00.模块总览.md) §十 | 🔧 部分 | **已落地**：`engine/context.py` 附件注入、`tools/image_tools.py:image.extract_waybill`（视觉识别）、录单员 `system_prompt` 追问澄清、`file.map_columns` 的 `constant_fields`、`ChatMessage.content` 多模态、纯附件发送。**待建**：PDF 运单（转图后识别）、表格→多业务对象（车辆/客户批量建档）、前端识别结果行内可编辑 | 视觉 Provider 配置（运营）、`waybill.batch_create`（✅） |
| 12 | [06.AI 数字人模块/03.附件与文件解析](../02.需求文档/02.企业端/06.AI%20数字人模块/03.附件与文件解析.md) | ✅ 已落地 | 已成文：上传约束、附件注入机制、parse_excel/map_columns（含 constant_fields）、image.extract_waybill、录单工作流与追问澄清、部署与测试要点 | §十（✅） |

### 2.6 计费引擎 - 承运商运费引擎

> 收入侧运费引擎（运价合同/运费计算）与支出侧成本引擎（任务支出成本自动计算，含司机侧费用项）均已落地。现补齐第三方向：以「承运商合同」为核心、承运商维度独立可管理的**承运商运费自动计算引擎**（承运商合同 → 承运价规则 → 运费记录），派车给承运商（`carrier_type=2`）时自动计算整单运费并回填 `task.carrier_cost_amount`，与成本引擎的司机侧费用项按 `carrier_type` 二选一分工，避免双算。

| # | 需求文档 | 状态 | 缺口明细 | 依赖 |
|---|---------|------|---------|------|
| 13 | [04.计费引擎模块/05.承运商运费自动计算系统设计文档](../02.需求文档/02.企业端/04.计费引擎模块/05.承运商运费自动计算系统设计文档.md) | ✅ 已落地 | 已落地（与客户收入侧对称、最大化复用 `freight_matcher` 算法层，顶层主键分替换为 `CARRIER_SCORE`）：模型 `biz_carrier_contract`/`biz_carrier_rate`/`biz_carrier_freight_result(_detail)`/`biz_carrier_freight_calc_task`/`biz_carrier_freight_calc_exception`/`biz_carrier_rate_change_log`（已注册）；服务 `CarrierFreightMatcher`/`CarrierFreightCalcService`/`CarrierFreightCalcTaskService`/`CarrierContractService`/`CarrierRateService`/`CarrierFreightExceptionService`；API `/billing/carrier-contract`、`/billing/carrier-rate`、`/billing/carrier-freight/*`、`/billing/carrier-freight-calc/{tasks,exceptions}`（均挂 `billing_carrier_freight` 门控）；独立 `carrier_freight_calc_worker` + `app.workers.carrier_freight_calc_main`；派车按 `carrier_type` 二选一分工触发 `_try_enqueue_billing_calc`，成本引擎在承运商任务上停用 `carrier_freight` 费用项；`billing_carrier_freight` 功能 + 菜单已写入 platform_sync 快照（含 required_tables 自动建表）。前端 `views/billing/carrier-contract/`（合同列表 `index.vue` + 详情 `detail.vue`：承运价明细 CRUD、`rate-edit`/`rate-version-history`/`rate-search`、`freight-preview` 运费试算、受影响任务重算）+ API 层 `api/billing/carrier-contract/` + 详情静态路由 `BillingCarrierContractDetail` 均已落地，与客户「运价合同」页对称。**待建**：任务详情页「承运运费」展示区块（可直接复用 `getTaskCarrierFreightResult`） | 收入侧引擎（✅）、成本引擎（✅）、承运商档案 `biz_carrier`（✅）、线路里程 `biz_route`（✅） |

---

## 三、全模块落地状态总表（对照参考）

> 用于快速判断"哪些已建、哪些待建"，✅ 项不在 §二 展开。

### 3.1 运营后台（Console）

| 需求文档 | 状态 | 代码位置 / 备注 |
|---------|------|----------------|
| 01.账号体系 | ✅ | `console/auth`、多角色账号 |
| 02.客户运营中心 | ✅ | 生命周期/跟进池 |
| 02.平台基础资源管理 | ✅ | 地区库、品牌/车系（autohome 同步）、经销商同步 |
| 03.系统日志 / 10.操作日志 | ✅ | `operation_log` 装饰器 + 日志中心 |
| 04.AI 数字员工（00~05） | ✅ | `modules/ai`（员工/工具/提示词/Provider/调用观测） |
| 08.基础数据管理/经销商同步 | ✅ | `dealer_sync_runner`、`autohome_sync` |
| 09.产品管理 | ✅ | 产品版本/功能清单/版本-功能关联 |

### 3.2 企业端（Client）

| 需求文档 | 状态 | 代码位置 / 备注 |
|---------|------|----------------|
| 01.首页工作台/快捷操作 | ✅ | `workbench`（待办/企业动态/快捷操作） |
| 01.客户端菜单架构重构 | ✅ | 菜单 v2 |
| 02.资源管理（车辆/驾驶员/社会运力池/运力中心） | ✅ | `capacity`（含 `operation_status`、承包结算配置） |
| 03.基础数据管理/地区 | ✅ | `region` |
| 04.计费引擎-收入侧（运价合同/运费计算/线路） | ✅ | `billing`、`route_service`（含高德驾车里程） |
| 04.计费引擎-支出侧（任务支出成本自动计算） | ✅ | `billing`（`cost_policy`/`cost_rule`/`task_cost_result`/`cost_calc_exception` 模型 + `CostMatcher`/`TaskCostCalcService`/`CostFinanceAdapter` + `cost_calc_worker`）；派车触发自动算 + 回填 `task.carrier_cost_amount`；前端 `views/billing/cost-policy/` + 任务详情应付成本区块 |
| **04.计费引擎-承运商运费引擎** | ✅ | **见 §2.6**：承运商合同 → 承运价规则 → 运费记录，与客户收入侧对称的独立引擎；后端模型/服务/API/Worker/派车触发/边界切分/功能门控 + 前端 `views/billing/carrier-contract/`（合同列表/详情/承运价编辑/试算）均已落地 |
| 05.合作伙伴（客户/承运商/平台互联-账号互通） | ✅ | `partner`（`carrier_invitation` / `carrier_invite_service`、平台互通收件箱） |
| 06.AI 数字人（对话页 + 录单员增强） | ✅ | `ai/api/client`；录单员支持 Excel/CSV 解析、运单图片视觉识别（`image.extract_waybill`）、客户缺失追问澄清（后续件见 §2.5） |
| 06.运营调度（任务单/状态机/调令/装卸/回单/逆向/已完成） | ✅ | `task`、`state_machine`、`waybill_receipt`、装卸记录 |
| 07.财务结算（00 总览/01 基座/05 任务级/06 承包单） | ✅ | `finance` 基座 + `task_finance` + 承包单；00 总览已于 2026-08-13 重构信息架构与分期 |
| 07.财务结算（09 一致性核对） | ✅ | `biz_recon_diff` + `recon/consistency_checker.py` + `base/finance_doc_service.py` + 两侧 linkage（第 1 期）；客户侧 `line_detector`、漏挂检测、差异 API 与对账工作台 UI（第 2 期）；应付侧 `ReconBinding` 与行级检测器（第 3 期） |
| 07.财务结算（10 资金收付 / 12 应收账龄） | ✅ | 收款核销 `biz_receipt_voucher(_settle_link)`（第 2 期）+ `biz_bank_account` / `biz_payment_batch(_item)` 与出纳台、资金流水、付款日历（第 4 期）；账龄与信用预警（`aging_service` + `credit_alert_service` + `/finance/ar-aging` 页 + 录单提示条） |
| 07.财务结算（08 岗位工作台） | ✅ | 三台齐活：对账工作台（`recon/workbench_service.py`）、出纳工作台、票据工作台 |
| 07.财务结算（11 进项发票 / 02 §5 销项发票 / 13 经营核算） | ✅ | `biz_vendor_invoice(_item/_settle_link)`（第 3 期）、`biz_customer_invoice(_item/_settle_link)`（第 4 期）、`accounting/` 三文件 + `/finance/profit` 重做（第 4 期） |
| 07.财务结算（07 驾驶员资金账户「往来账」） | ✅ | `capacity/self_capacity/driver/driver_fund_account(_service)` + `biz_driver_fund_account`/`biz_driver_fund_transaction`；预付单支付/撤销经 `DriverFundOrchestrator` 自动写/冲正流水；司机 H5 只读；测试 `tests/test_driver_fund_account.py` |
| 07.财务结算（02 客户侧对账结算） | ✅ | `biz_customer_recon(_waybill_link)` / `biz_customer_settlement(_recon_link)` + 三条 service + 前端 `views/finance/customer-recon/`、`customer-settlement/` |
| 07.财务结算（03 承运商 / 04 司机工资） | ✅ | `biz_carrier_recon(_task_link)` / `biz_carrier_settlement_doc(_recon_link)` / `biz_driver_payroll(_task_link/_item)` + 三条 service + 前端三页（第 3 期） |
| **07.财务结算（13 §6 主体内部往来）** | ⏸ | **未立项**：定价口径未拍板；核算页已提供跨主体只读明细，**见 §2.1 #14** |
| 08.审批中心（引擎/接入/流程配置/组织扩展/实施） | ✅ | `approval`（flow/instance/task/engine/resolver/condition） |
| 10.系统管理/组织架构与权限 | ✅ | `organization`、`role` |
| **10.系统管理/经营主体（BusinessEntity）** | 🔧 | `organization/business_entity(_service)` + `biz_business_entity`；司机/车辆/运力/任务/费用单/资金账户按主体归属；客户/承运商/运单收入归属维度已补齐（迁移 `20260706_002`~`005`）。**待建**：前端主体选择器/跨主体软提示、内部往来结算引擎（**见 §2.2**） |
| 11.系统设置/系统水印 | ✅ | 水印功能 |

### 3.3 移动端 / LITE / 运力宝 / 官网

| 需求文档 | 状态 | 代码位置 / 备注 |
|---------|------|----------------|
| 03.移动端/驾驶员 H5 端（00~05） | ✅ | `frontend/driver-h5`（登录/多企业/任务/财务/个人中心） |
| **03.移动端/BI 数据看板** | 📋 | **见 §2.3** |
| **03.移动端/业务模块（管理端）** | 🔧 | **见 §2.3** |
| **03.LITE 端/承运商运力上报** | 📋 | **见 §2.3** |
| 06.运力宝/02 证照监控引擎 | ✅ | `compliance`（扫描/告警） |
| 06.运力宝/03 承运商运力建档 | ✅ | `carrier_capacity`（含审批回调） |
| **06.运力宝/04 商业化守卫与配额** | 🔧 | **见 §2.4** |
| 04.产品官网/官网内容升级 | ✅ | `frontend/website` |

---

## 四、建议排期与优先级

> 结合业务闭环价值与依赖关系给出建议，具体节奏以实际资源为准。

| 优先级 | 事项 | 理由 | 前置依赖 |
|:------:|------|------|---------|
| ✅ | 财务公共底座：通用服务模板 + 候选池 linkage + 一致性核对器（§2.1 #1/#2） | **2026-08-13 已落地**。互斥校验已统一收口，不再分散三处 | 通用基座（✅） |
| ✅ | 财务-客户侧对账/结算 + 收款核销 + 应收账龄 + 对账工作台（§2.1 #3/#4/#5/#11） | **2026-08-14 已落地**（第 2 期 A~F，含四个前端页面） | 公共底座（✅） |
| ✅ | 财务-承运商侧对账/结算 + 自有司机工资单 + 进项发票（§2.1 #6/#7/#8） | **2026-08-14 已落地**（第 3 期 A~F，含四个前端页面） | 公共底座（✅）、任务级结算单（✅） |
| ✅ | 财务-销项发票 + 银行账户与打款批次 + 出纳台与票据台 + 经营核算（§2.1 #9~#12） | **2026-08-14 已落地**（第 4 期 A~F，含六个前端页面） | 上述各项 |
| P1 | 财务-写操作幂等键（§2.1 #15） | 付款登记、发薪、批次执行都是不可逆动作，网络重试会造成重复出款；schema 已预留，只缺服务端校验 | 全模块已落地（✅） |
| P2 | 财务-核算页与驾驶舱互链 + 分摊工具收口（§2.1 #12 的两项刻意未做） | 两处口径说明文案定稿后再动；分摊收口要连带回归驾驶舱数字 | 经营核算（✅） |
| P2 | 经营主体前端选择器 + 跨主体软提示（§2.2 #5） | 让客户/承运商/运单可维护主体归属并在跨主体时提示（不拦截）；归属维度后端已就位 | 经营主体维度（✅） |
| — | 经营主体内部往来（关联交易）结算引擎（§2.2 #5） | **暂不立项**：定价口径未定，先建表必返工。第 4 期先在经营核算里单列「跨主体待结转」满足看数需求 | 业务确认定价口径 + §2.1 应收/应付单据 |
| P2 | 运力宝商业化守卫与配额补全（§2.4 #9/#10） | 决定运力宝可售卖，门控/配额底座已具备 | `require_feature`/`quota_service`（✅） |
| P2 | LITE 端承运商运力上报（§2.3 #8） | 承运商轻量协同入口，后端能力已就绪 | `carrier_capacity`/`carrier_invitation`（✅） |
| P3 | 移动端管理侧 + 移动端 BI 看板（§2.3 #6/#7） | 移动化增强，非阻塞主业务闭环 | 相关 BI/调度后端（✅） |
| P3 | AI 录单员后续件：PDF 识别 / 多业务对象 / 识别结果行内编辑（§2.5 #11） | 录单闭环已通，后续为体验与覆盖面增强 | 录单核心（✅）、视觉 Provider 配置 |

---

## 五、备注

- 本清单聚焦"文档已有、代码待建"，不覆盖"代码已有、文档待补"（后者已在近期文档迭代中处理）。
- 财务模块的详细字段/状态机/桥接结构已在 [07.财务结算模块](../02.需求文档/02.企业端/07.财务结算模块/00.模块总览.md) 各文档中定义，开发时**直接照文档落地即可**，无需二次设计。
- 与 [开发路线图](开发路线图.md) 的关系：路线图是早期阶段规划（部分已过时），本清单以"当前代码实况"为基准，二者如有冲突以本清单为准。
