[pytest]
; 纯单元测试（状态机等零 DB 依赖逻辑）配置。
; 把 backend 根目录加入 import 路径，使 `import app.xxx` 可用。
pythonpath = .
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
addopts = -ra -q
