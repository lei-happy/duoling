# 需求-代码落地差距清单（开发计划）

> **创建日期**：2026-07-06
>
> **更新日期**：2026-07-07（计费引擎「承运商运费自动计算引擎」后端全链路落地：模型/服务/API/Worker/派车触发/成本引擎边界切分/`billing_carrier_freight` 功能门控+菜单快照均已就绪，前端页面待建，见 §2.6）

> **历史更新**：2026-07-07（计费引擎新增「承运商运费自动计算引擎」设计：承运商合同 → 承运价规则 → 运费记录，与客户收入侧对称、复用 `freight_matcher` 算法层，派车给承运商自动算整单运费并回填 `task.carrier_cost_amount`，见 §2.6）

> **历史更新**：2026-07-07（AI 录单数字人能力增强：附件注入上下文 + 运单图片视觉识别（`image.extract_waybill`）+ 客户缺失主动追问澄清 + `map_columns` 常量回填，打通 Excel/图片双录单闭环）
>
> **历史更新**：2026-07-07（计费引擎「支出成本自动计算」支出侧全链路落地；经营主体（BusinessEntity）概念落地，并扩展客户/承运商/运单「收入归属」维度，明确跨主体不拦截策略）
>
> **历史更新**：2026-07-06（驾驶员资金账户「往来账」模块落地：账户本体 + 手工流水 + 司机 H5 只读 + 预付单支付/撤销自动流水联动）
>
> **文档定位**：盘点"**需求文档已有、但代码尚未开发或开发进行中**"的模块，作为后续排期与资源投入的依据。已完整落地的模块只在 §三 状态总表中列出，不展开。
>
> **维护约定**：每完成一个缺口项，更新其状态并从 §二 待办清单移除；新识别的缺口追加到 §二。
>
> **状态标记**：✅ 已落地 ｜ 🔧 部分落地（开发进行中）｜ 📐 仅模型/设计 ｜ 📋 未开发（仅需求文档）
>
> **判定口径**：以 `backend/app/modules/**`（models / services / api）与 `frontend/*`（views）实际代码为准，逐一比对需求文档。

---

## 一、结论速览

- 绝大多数需求文档对应的代码**已经落地**：账号体系、客户运营中心、AI 数字员工/数字人、资源管理（车辆/驾驶员/运力中心/社会运力池）、计费引擎**收入侧**（运价合同/运费计算/线路）、**计费引擎支出侧（任务支出成本自动计算）**、合作伙伴（客户/承运商/平台互联）、运营调度（任务单/状态机/调令/装卸/回单）、审批中心、系统设置（水印）、运力宝（证照监控/承运商建档）、**驾驶员资金账户「往来账」（含预付单支付/撤销自动流水联动）**、**经营主体（BusinessEntity）多主体归属维度**、驾驶员 H5 端等。
- **核心缺口集中在两块**：
  1. **财务结算模块的"应收 + 跨任务应付"三条线**（客户侧对账/结算/发票、承运商侧对账/结算、自有司机工资单）——通用基座已就绪，具体单据未建。经营主体维度已打通，主体间差额（关联交易/内部往来）的结算引擎待建。
  2. **移动端管理侧 / 移动端 BI 看板 / LITE 端承运商运力上报**——需求文档已有，前端工程未建。

---

## 二、待办清单（需求已有、代码未开发或进行中）

### 2.1 财务结算模块（最高优先级）

> 通用基座（`FinanceDocBaseMixin` / `FinanceDocEvent` / `FinanceStateMachine` / `FinanceDocEventWriter` / `LockOrchestrator`）与任务级费用单（预付/补款/结算/承包单）**已落地**；以下三条线仅有需求文档，尚无 `models/finance/` 具体单据表、桥接表与服务。

| # | 需求文档 | 状态 | 缺口明细 | 依赖 |
|---|---------|------|---------|------|
| 1 | [07.财务结算/02.客户侧-对账与结算与发票](../02.需求文档/02.企业端/07.财务结算模块/02.客户侧-对账与结算与发票.md) | 📋 未开发 | 缺 `customer_recon` / `customer_settlement` / `customer_invoice` 模型 + 3 张桥接表；缺应收状态机接入、开票/红冲、`waybill.is_locked` 回灌 | 通用基座（✅）、运单签收聚合（✅） |
| 2 | [07.财务结算/03.承运商侧-对账与结算](../02.需求文档/02.企业端/07.财务结算模块/03.承运商侧-对账与结算.md) | 📋 未开发 | 缺 `carrier_recon` / `carrier_settlement_doc` 模型 + 桥接表；缺跨任务批量对账、与任务级结算单互斥校验、`is_recon_bound` 联动 | 通用基座（✅）、任务级结算单（✅） |
| 3 | [07.财务结算/04.自有司机工资单](../02.需求文档/02.企业端/07.财务结算模块/04.自有司机工资单.md) | 📋 未开发 | 缺 `driver_payroll` + 任务桥接 + 工资项子表；缺周期聚合发薪、`payroll_settled` 联动。**预付抵扣 / 司机账户余额闭环所需的「往来账」底座已由驾驶员资金账户（✅）提供**，工资单可直接读账户余额并写核销流水 | 通用基座（✅）、驾驶员资金账户「往来账」（✅）、承包结算配置（✅） |
| 4 | 财务通用基座后续件（服务层） | 🔧 部分 | `services/finance/base/finance_doc_service.py` 通用服务模板未建；`linkage/task_to_finance.py`、`linkage/waybill_to_finance.py` 候选池生成未建（当前任务级为内联实现） | 支撑 #1~#3 |

> 参考：[01.财务单据体系与业务单据联动设计](../02.需求文档/02.企业端/07.财务结算模块/01.财务单据体系与业务单据联动设计.md) §9 已标注各文件的 ✅/🕒 落地状态。

### 2.2 经营主体（BusinessEntity）多主体归属

> 经营主体（法人/独立核算单元）已落地：`biz_business_entity` 表 + `BusinessEntityService`（默认主体维护/CRUD/删除引用保护）+ API + 前端「系统管理/经营主体」。资源侧（司机/车辆/运力/任务/费用单）与资金账户（`owner_type + owner_id + enterprise_id`）已按主体归属；本次进一步补齐**收入侧归属维度**（客户/承运商/运单默认主体带出）。以下为剩余缺口。

| # | 需求文档 | 状态 | 缺口明细 | 依赖 |
|---|---------|------|---------|------|
| 5 | [10.系统管理/02.经营主体管理](../02.需求文档/02.企业端/10.系统管理模块/02.经营主体管理.md) | 🔧 部分 | **已落地**：`biz_customer` / `biz_carrier` / `biz_waybill` 新增 `enterprise_id`（迁移 `20260706_005`）；运单创建从客户默认主体带出收入归属、承运商任务成本归属继承承运商档案主体；明确「跨主体不拦截」（收入主体≠成本主体时不阻断，如实两侧记账）。**待建**：前端客户/承运商/运单表单的经营主体选择器与跨主体软提示；主体间差额的**内部往来（关联交易）结算引擎**；应收单据按主体分账（并入 §2.1 #1） | 经营主体基座（✅）、双维归属（收入=运单 / 成本=任务，✅） |

### 2.3 移动端 / LITE 端

| # | 需求文档 | 状态 | 缺口明细 |
|---|---------|------|---------|
| 6 | [03.移动端/BI数据看板](../02.需求文档/03.移动端/BI数据看板.md) | 📋 未开发 | `frontend/driver-h5` 无 BI/看板页面；移动端数据看板未建 |
| 7 | [03.移动端/业务模块](../02.需求文档/03.移动端/业务模块.md) | 🔧 部分 | 驾驶员 H5 端已落地（登录/多企业切换/任务流转/财务收入/个人中心）；**管理人员移动端**（调度/审批移动侧）尚未建工程 |
| 8 | [03.LITE端/承运商运力上报](../02.需求文档/03.LITE端/承运商运力上报.md) | 📋 未开发 | 无 `frontend/lite` 工程；承运商轻量上报入口未建（后端 `carrier_capacity` / `carrier_invitation` 已具备承接能力） |

### 2.4 运力宝商业化

| # | 需求文档 | 状态 | 缺口明细 |
|---|---------|------|---------|
| 9 | [06.运力宝/04.商业化基建-功能守卫与配额](../02.需求文档/06.运力宝/04.商业化基建-功能守卫与配额.md) | 🔧 部分 | `core/permissions.py` 的 `require_feature` 功能门控 + `quota_service` 版本配额校验已落地；**按运力宝业务定义的配额项、超限提示、商业化埋点/守卫覆盖面**待补全并逐接口接入 |
| 10 | [06.运力宝/01.产品方案与商业化设计](../02.需求文档/06.运力宝/01.产品方案与商业化设计.md) | 🔧 部分 | 证照监控（✅ `compliance`）、承运商运力建档（✅ `carrier_capacity`）已落地；**商业化转化闭环（试用/购买/额度售卖）**待确认落地范围 |

> 运力宝证照监控引擎（02）、承运商运力建档（03）已落地；[05.部署与运维手册](../02.需求文档/06.运力宝/05.部署与运维手册.md) 属运维文档，非代码开发项。

### 2.5 AI 数字人 - 录单员能力增强

> 2026-07-07 已落地「附件注入 + 运单图片视觉识别 + 追问澄清」核心闭环（详见 [06.AI 数字人模块/00.模块总览](../02.需求文档/02.企业端/06.AI%20数字人模块/00.模块总览.md) §十）。以下为剩余后续件。

| # | 需求文档 | 状态 | 缺口明细 | 依赖 |
|---|---------|------|---------|------|
| 11 | [06.AI 数字人模块/00.模块总览](../02.需求文档/02.企业端/06.AI%20数字人模块/00.模块总览.md) §十 | 🔧 部分 | **已落地**：`engine/context.py` 附件注入、`tools/image_tools.py:image.extract_waybill`（视觉识别）、录单员 `system_prompt` 追问澄清、`file.map_columns` 的 `constant_fields`、`ChatMessage.content` 多模态、纯附件发送。**待建**：PDF 运单（转图后识别）、表格→多业务对象（车辆/客户批量建档）、前端识别结果行内可编辑 | 视觉 Provider 配置（运营）、`waybill.batch_create`（✅） |
| 12 | [06.AI 数字人模块/03.附件与文件解析](../02.需求文档/02.企业端/06.AI%20数字人模块/03.附件与文件解析.md) | ✅ 已落地 | 已成文：上传约束、附件注入机制、parse_excel/map_columns（含 constant_fields）、image.extract_waybill、录单工作流与追问澄清、部署与测试要点 | §十（✅） |

### 2.6 计费引擎 - 承运商运费引擎

> 收入侧运费引擎（运价合同/运费计算）与支出侧成本引擎（任务支出成本自动计算，含司机侧费用项）均已落地。现补齐第三方向：以「承运商合同」为核心、承运商维度独立可管理的**承运商运费自动计算引擎**（承运商合同 → 承运价规则 → 运费记录），派车给承运商（`carrier_type=2`）时自动计算整单运费并回填 `task.carrier_cost_amount`，与成本引擎的司机侧费用项按 `carrier_type` 二选一分工，避免双算。

| # | 需求文档 | 状态 | 缺口明细 | 依赖 |
|---|---------|------|---------|------|
| 13 | [04.计费引擎模块/05.承运商运费自动计算系统设计文档](../02.需求文档/02.企业端/04.计费引擎模块/05.承运商运费自动计算系统设计文档.md) | ✅ 后端已落地 | 已落地（与客户收入侧对称、最大化复用 `freight_matcher` 算法层，顶层主键分替换为 `CARRIER_SCORE`）：模型 `biz_carrier_contract`/`biz_carrier_rate`/`biz_carrier_freight_result(_detail)`/`biz_carrier_freight_calc_task`/`biz_carrier_freight_calc_exception`/`biz_carrier_rate_change_log`（已注册）；服务 `CarrierFreightMatcher`/`CarrierFreightCalcService`/`CarrierFreightCalcTaskService`/`CarrierContractService`/`CarrierRateService`/`CarrierFreightExceptionService`；API `/billing/carrier-contract`、`/billing/carrier-rate`、`/billing/carrier-freight/*`、`/billing/carrier-freight-calc/{tasks,exceptions}`（均挂 `billing_carrier_freight` 门控）；独立 `carrier_freight_calc_worker` + `app.workers.carrier_freight_calc_main`；派车按 `carrier_type` 二选一分工触发 `_try_enqueue_billing_calc`，成本引擎在承运商任务上停用 `carrier_freight` 费用项；`billing_carrier_freight` 功能 + 菜单已写入 platform_sync 快照（含 required_tables 自动建表）。**待建**：前端「承运商合同」页（`views/billing/carrier-contract/`）与任务详情承运运费区块 | 收入侧引擎（✅）、成本引擎（✅）、承运商档案 `biz_carrier`（✅）、线路里程 `biz_route`（✅） |

---

## 三、全模块落地状态总表（对照参考）

> 用于快速判断"哪些已建、哪些待建"，✅ 项不在 §二 展开。

### 3.1 运营后台（Console）

| 需求文档 | 状态 | 代码位置 / 备注 |
|---------|------|----------------|
| 01.账号体系 | ✅ | `console/auth`、多角色账号 |
| 02.客户运营中心 | ✅ | 生命周期/跟进池 |
| 02.平台基础资源管理 | ✅ | 地区库、品牌/车系（autohome 同步）、经销商同步 |
| 03.系统日志 / 10.操作日志 | ✅ | `operation_log` 装饰器 + 日志中心 |
| 04.AI 数字员工（00~05） | ✅ | `modules/ai`（员工/工具/提示词/Provider/调用观测） |
| 08.基础数据管理/经销商同步 | ✅ | `dealer_sync_runner`、`autohome_sync` |
| 09.产品管理 | ✅ | 产品版本/功能清单/版本-功能关联 |

### 3.2 企业端（Client）

| 需求文档 | 状态 | 代码位置 / 备注 |
|---------|------|----------------|
| 01.首页工作台/快捷操作 | ✅ | `workbench`（待办/企业动态/快捷操作） |
| 01.客户端菜单架构重构 | ✅ | 菜单 v2 |
| 02.资源管理（车辆/驾驶员/社会运力池/运力中心） | ✅ | `capacity`（含 `operation_status`、承包结算配置） |
| 03.基础数据管理/地区 | ✅ | `region` |
| 04.计费引擎-收入侧（运价合同/运费计算/线路） | ✅ | `billing`、`route_service`（含高德驾车里程） |
| 04.计费引擎-支出侧（任务支出成本自动计算） | ✅ | `billing`（`cost_policy`/`cost_rule`/`task_cost_result`/`cost_calc_exception` 模型 + `CostMatcher`/`TaskCostCalcService`/`CostFinanceAdapter` + `cost_calc_worker`）；派车触发自动算 + 回填 `task.carrier_cost_amount`；前端 `views/billing/cost-policy/` + 任务详情应付成本区块 |
| **04.计费引擎-承运商运费引擎** | ✅ | **见 §2.6**：承运商合同 → 承运价规则 → 运费记录，与客户收入侧对称的独立引擎；后端模型/服务/API/Worker/派车触发/边界切分/功能门控均已落地（前端页面待建） |
| 05.合作伙伴（客户/承运商/平台互联-账号互通） | ✅ | `partner`（`carrier_invitation` / `carrier_invite_service`、平台互通收件箱） |
| 06.AI 数字人（对话页 + 录单员增强） | ✅ | `ai/api/client`；录单员支持 Excel/CSV 解析、运单图片视觉识别（`image.extract_waybill`）、客户缺失追问澄清（后续件见 §2.5） |
| 06.运营调度（任务单/状态机/调令/装卸/回单/逆向/已完成） | ✅ | `task`、`state_machine`、`waybill_receipt`、装卸记录 |
| 07.财务结算（00 总览/01 基座/05 任务级/06 承包单） | ✅ | `finance` 基座 + `task_finance` + 承包单 |
| 07.财务结算（07 驾驶员资金账户「往来账」） | ✅ | `capacity/self_capacity/driver/driver_fund_account(_service)` + `biz_driver_fund_account`/`biz_driver_fund_transaction`；预付单支付/撤销经 `DriverFundOrchestrator` 自动写/冲正流水；司机 H5 只读；测试 `tests/test_driver_fund_account.py` |
| **07.财务结算（02 客户/03 承运商/04 司机工资）** | 📋 | **见 §2.1** |
| 08.审批中心（引擎/接入/流程配置/组织扩展/实施） | ✅ | `approval`（flow/instance/task/engine/resolver/condition） |
| 10.系统管理/组织架构与权限 | ✅ | `organization`、`role` |
| **10.系统管理/经营主体（BusinessEntity）** | 🔧 | `organization/business_entity(_service)` + `biz_business_entity`；司机/车辆/运力/任务/费用单/资金账户按主体归属；客户/承运商/运单收入归属维度已补齐（迁移 `20260706_002`~`005`）。**待建**：前端主体选择器/跨主体软提示、内部往来结算引擎（**见 §2.2**） |
| 11.系统设置/系统水印 | ✅ | 水印功能 |

### 3.3 移动端 / LITE / 运力宝 / 官网

| 需求文档 | 状态 | 代码位置 / 备注 |
|---------|------|----------------|
| 03.移动端/驾驶员 H5 端（00~05） | ✅ | `frontend/driver-h5`（登录/多企业/任务/财务/个人中心） |
| **03.移动端/BI 数据看板** | 📋 | **见 §2.3** |
| **03.移动端/业务模块（管理端）** | 🔧 | **见 §2.3** |
| **03.LITE 端/承运商运力上报** | 📋 | **见 §2.3** |
| 06.运力宝/02 证照监控引擎 | ✅ | `compliance`（扫描/告警） |
| 06.运力宝/03 承运商运力建档 | ✅ | `carrier_capacity`（含审批回调） |
| **06.运力宝/04 商业化守卫与配额** | 🔧 | **见 §2.4** |
| 04.产品官网/官网内容升级 | ✅ | `frontend/website` |

---

## 四、建议排期与优先级

> 结合业务闭环价值与依赖关系给出建议，具体节奏以实际资源为准。

| 优先级 | 事项 | 理由 | 前置依赖 |
|:------:|------|------|---------|
| P0 | 财务-客户侧对账/结算/发票（§2.1 #1） | 应收是资金回笼的核心闭环，当前完全缺失 | 通用基座（✅）、运单签收聚合（✅） |
| P0 | 财务通用服务模板 + 候选池 linkage（§2.1 #4） | 是 #1~#3 的公共底座，先建可显著降低重复 | 通用基座（✅） |
| P1 | 财务-承运商侧对账/结算（§2.1 #2） | 应付跨任务批量结算，月结承运商刚需 | #4、任务级结算单（✅） |
| P1 | 财务-自有司机工资单（§2.1 #3） | 自有司机发薪闭环，联动承包结算配置；往来账底座已就位可直接对接预付抵扣/余额核销 | #4、承包结算配置（✅）、驾驶员资金账户「往来账」（✅） |
| P2 | 经营主体前端选择器 + 跨主体软提示（§2.2 #5） | 让客户/承运商/运单可维护主体归属并在跨主体时提示（不拦截）；归属维度后端已就位 | 经营主体维度（✅） |
| P2 | 经营主体内部往来（关联交易）结算引擎（§2.2 #5） | 收入主体≠成本主体时的主体间差额结算，依赖应收/应付单据分账 | §2.1 应收/应付单据 |
| P2 | 运力宝商业化守卫与配额补全（§2.4 #9/#10） | 决定运力宝可售卖，门控/配额底座已具备 | `require_feature`/`quota_service`（✅） |
| P2 | LITE 端承运商运力上报（§2.3 #8） | 承运商轻量协同入口，后端能力已就绪 | `carrier_capacity`/`carrier_invitation`（✅） |
| P3 | 移动端管理侧 + 移动端 BI 看板（§2.3 #6/#7） | 移动化增强，非阻塞主业务闭环 | 相关 BI/调度后端（✅） |
| P3 | AI 录单员后续件：PDF 识别 / 多业务对象 / 识别结果行内编辑（§2.5 #11） | 录单闭环已通，后续为体验与覆盖面增强 | 录单核心（✅）、视觉 Provider 配置 |

---

## 五、备注

- 本清单聚焦"文档已有、代码待建"，不覆盖"代码已有、文档待补"（后者已在近期文档迭代中处理）。
- 财务模块的详细字段/状态机/桥接结构已在 [07.财务结算模块](../02.需求文档/02.企业端/07.财务结算模块/00.模块总览.md) 各文档中定义，开发时**直接照文档落地即可**，无需二次设计。
- 与 [开发路线图](开发路线图.md) 的关系：路线图是早期阶段规划（部分已过时），本清单以"当前代码实况"为基准，二者如有冲突以本清单为准。
