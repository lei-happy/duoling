# 租户业务库（zt_biz_{code}）数据库设计

## 概述

每个企业客户拥有独立的业务数据库，存储该企业的用户、组织、车辆、驾驶员、运单等业务数据。

**数据库命名**：
- 开发环境：`zt_biz_{tenant_code}_ci`（如 `zt_biz_1001_ci`）
- 生产环境：`zt_biz_{tenant_code}`（如 `zt_biz_1001`）

## 表清单

| 序号 | 表名 | 说明 | ORM 模型 | 表层级 | 模型状态 |
|------|------|------|----------|--------|----------|
| 1 | biz_user | 业务用户表 | `BizUser` | core | ✅ 已实现 |
| 2 | biz_role | 业务角色表 | `BizRole` | core | ✅ 已实现 |
| 3 | biz_menu | 业务菜单表 | `BizMenu` | core | ✅ 已实现 |
| 4 | biz_role_menu | 角色菜单关联表 | - | core | ⚠️ 仅有 SQL，ORM 模型待补充 |
| 5 | biz_user_role | 用户角色关联表 | - | core | ⚠️ 仅有 SQL，ORM 模型待补充 |
| 6 | biz_department | 组织架构/部门表 | - | core | ⚠️ 仅有 SQL，ORM 模型待补充 |
| 7 | biz_system_config | 系统配置表 | `SystemConfig` | core | 🆕 待实现 |
| 8 | biz_vehicle | 车辆核心表 | `Vehicle` | business | 🔄 待重构（拆分为核心+扩展） |
| 9 | biz_vehicle_ext | 车辆扩展信息表 | `VehicleExt` | business | 🆕 待实现 |
| 10 | biz_trailer | 挂车核心表 | `Trailer` | business | 🆕 待实现 |
| 11 | biz_trailer_ext | 挂车扩展信息表 | `TrailerExt` | business | 🆕 待实现 |
| 12 | biz_driver | 驾驶员信息表 | `Driver` | business | ✅ 已实现 |
| 13 | biz_customer | 客户信息表 | `Customer` | business | 🔄 待扩展（增加结算方式等字段） |
| 14 | biz_route | 路线表 | `Route` | business | ✅ 已实现 |
| 15 | biz_order | 运单表（旧） | `Order` | business | ⚠️ 已废弃，由 biz_waybill 替代 |
| 16 | biz_waybill | 运单表（商品车运输） | `Waybill` | business | 🆕 待实现 |
| 17 | biz_freight_contract | 运价合同表 | `FreightContract` | business | 🆕 待实现 |
| 18 | biz_freight_rate | 运价明细表 | `FreightRate` | business | 🆕 待实现 |

共 **18** 张表。

> **待办**：
> - 需补充 `biz_role_menu`、`biz_user_role`、`biz_department` 三个表对应的 SQLAlchemy ORM 模型文件。
> - 需重构 `biz_vehicle` 模型（拆分为核心表+扩展表）。详见需求文档：[车辆管理PRD](../../02.需求文档/02.企业端/02.资源管理模块/01.车辆管理.md)。
> - 需扩展 `biz_customer` 模型（增加 customer_code/settlement_type/credit_code 等字段），迁移到 `partner/` 目录。详见需求文档：[客户管理PRD](../../02.需求文档/02.企业端/05.合作伙伴模块/01.客户管理.md)。
> - 新建 `biz_waybill`、`biz_freight_contract`、`biz_freight_rate`、`biz_system_config` 四张表。详见需求文档：[运单管理PRD](../../02.需求文档/02.企业端/05.业务模块-运单管理.md) 和 [计费引擎PRD](../../02.需求文档/02.企业端/04.计费引擎模块/01.运价合同管理.md)。

## 建表 SQL

完整建表 SQL 模板见：[sql/zt_biz_template.sql](sql/zt_biz_template.sql)

> 注意：实际部署时由后端 SQLAlchemy ORM 自动创建表结构（`metadata.create_all`）。SQL 文件仅用于文档记录和手动初始化场景。由于 `biz_role_menu`、`biz_user_role`、`biz_department` 缺少 ORM 模型，当前自动建表可能遗漏这 3 张表，需优先补充模型。

## 表关系说明

- `biz_user` 通过 `biz_user_role` 关联 `biz_role`（多对多）
- `biz_role` 通过 `biz_role_menu` 关联 `biz_menu`（多对多）
- `biz_user.department_id` 关联 `biz_department`
- `biz_vehicle.trailer_id` 关联 `biz_trailer`（可选 1:1）
- `biz_vehicle_ext.vehicle_id` 关联 `biz_vehicle`（1:1 扩展）
- `biz_trailer_ext.trailer_id` 关联 `biz_trailer`（1:1 扩展）
- `biz_driver.user_id` 关联 `biz_user`
- `biz_driver.department_id` 关联 `biz_department`
- `biz_order` 关联 `biz_vehicle`、`biz_driver`、`biz_customer`、`biz_route`（已废弃）
- `biz_waybill.customer_id` 关联 `biz_customer`（运单→客户）
- `biz_waybill.contract_id` 关联 `biz_freight_contract`（运单→运价合同）
- `biz_waybill.rate_id` 关联 `biz_freight_rate`（运单→运价明细）
- `biz_freight_contract.customer_id` 关联 `biz_customer`（合同→客户）
- `biz_freight_rate.contract_id` 关联 `biz_freight_contract`（运价明细→合同）

## 初始数据

新租户数据库创建时，自动插入以下初始数据（见 SQL 模板末尾）：

- 默认角色：管理员（admin）、操作员（operator）、驾驶员（driver）
- 默认部门：总公司（HQ）→ 运营部（OP）/ 车队部（FL）/ 财务部（FI）
- 企业超管账号（由平台在创建租户时指定）
