# 重新灌入产品功能清单（会自动跳过已存在的，只插入新增的）
python scripts/seed/seed_product_features.py

# 重新灌入客户端菜单（会清除旧菜单后全量重建）
python scripts/seed/seed_client_menus.py

# 启动后端  
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# 启动前端
npm run dev

# 启动前端
npm run dev

# 启动前端
npm run dev