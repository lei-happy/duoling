-- ============================================================
-- 智途(ZhiTu) 数据库初始化 SQL（线上环境）
-- 线上环境数据库名不带 _ci 后缀
-- ============================================================

CREATE DATABASE IF NOT EXISTS `zt_platform`
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;
